import React from "react";

class Loader extends React.Component {
    render() {
        return (
            <div className="ui active centered inline huge loader"/>
        );
    }
}

export default Loader;